---
title: 进度 2026-09-17：会话持久化与契约硬化
type: progress
status: done
date: 2026-09-17
updated: 2026-09-17
links: [../01-需求/PRD-实时交互数字人助手.md, ../02-方案/SPEC-接口与协议规范.md]
---
> 文件名：`PROGRESS-2026-09-17-会话持久化与契约硬化.md`
>
> 类型：progress
> 状态：已完成
> 读者：未来的自己 / 接手的人 / AI
> 结论：补齐用户实测暴露的"刷新丢历史"（会话本地持久化 + 存活探测端点），并对一份外部代码审查清单做了**核实后**的 4+1 条硬化（队列背压 / TTS 连接独占 / 常量收口 / 任务收尾 / 契约标注）；**真人麦克风链路验证仍未做**。

## 本阶段目标

接续 `PROGRESS-2026-09-15-实时链路接入与口型上云.md`：解决用户实测报的两个可感知问题（打不断已在上份记录、刷新丢历史在本份），并按项目定位（差异化在延迟/音画/并发）对外部审查清单做**选择性**落地。

## 已完成（MUST）

### 一、会话持久化（轻档，解决"刷新丢历史"）

- [x] 后端新增**会话存活探测** `GET /api/v1/session/{id}`（投 404 表示会话已失效）——只读、不改状态、不刷 `last_active`
- [x] 前端 `localStorage` 持久化会话与对话（只存 user/digital 气泡，system 调试信息不落盘，上限 50 条）
- [x] 恢复策略**区分两种情形**：探测命中 → 复用同一 `session_id`（**后端 `ctx.history` 还在，多轮上下文连续**）；探测 404（后端重启过）→ 新建会话但**保留本地历史**，并落一条 system 消息明确"上下文已重置"，不假装连续
- [x] "重连"按钮改为强制新建（`resetSession({ fresh: true })`）
- [x] 测试：新端点 3 项断言（含 404）；pytest **52 → 53 passed**

> 未做（明确记为"重档"，需要时再做）：服务端持久化（建表 + `/history` 端点），可支持跨浏览器/换设备恢复。

### 二、外部审查清单的核实与选择性硬化（4+1 条）

对 20 条清单**逐条核对代码**后，只落地与项目差异化方向（延迟/音画/并发）相关的项：

- [x] **#6 口型队列背压**（音画同步风险，且与 RUNBOOK §3.14 待验项吻合）：`lip_in_q` 限长（`config.yaml lip.queue_max`，默认 8）+ **满则丢最旧**；`done` 事件新增 `lip_dropped` 暴露丢帧数。理由：TTS 合成快于单 GPU 串行推理，无界队列会让片段落后音频时钟 → **丢帧好过整体延迟**
- [x] **#10 TTS 连接独占**：`CosyVoiceTts.stream` 加独占锁 + 按 `task_id` 过滤 JSON 事件。真因：DashScope 的**音频帧是裸二进制、不带 task_id**，接收侧无法区分 task → 并发用同一连接会串音频（当前 `tts_consumer` 串行，属防御性；一旦有人改并发就立刻出错）
- [x] **#2 常量收口**：新增 `src/constants.py`（`SAMPLE_RATE` / `BYTES_PER_MS` / `ms_to_bytes` / `bytes_to_ms`），收敛原先散落三处的 `32 字节/毫秒`（`routes.py` `*32`、`lip/engine.py` `32000.0`、`tts/cosyvoice.py` `BYTES_PER_MS`）。理由：CosyVoice2 原生 24kHz 时三处会同时错
- [x] **#11 任务收尾**：`chat_stream` 的 `finally` 从"只 cancel"改为 **cancel + `await gather(return_exceptions=True)`**，避免 CancelledError 无人接管与子任务收尾被跳过
- [x] **#20 契约标注**：SPEC §2 标注 `buffer_audio/buffer_lip` **P1 未落地**（实际队列是 `chat_stream` 局部变量）；`AsrChunk.confidence` 已在 v1.4 标注恒为 0

**核实后判定"不做"的（记下来避免重复讨论）**：

| 清单条目 | 判定 |
|---|---|
| #12 interrupt 不清理在途 TTS/Lip | **非缺陷**：DESIGN-打断 §3.3 规则2 明文规定"GPU 在途推理不取消，返回后丢弃"（PyTorch 无安全取消 API） |
| #8 offset 用字节反推"长句累积误差" | **描述不成立**：句起点每句都由服务端时间戳校正（`cursor_ms = sent_base + sent_dur`），误差不跨句累积 |
| #3 `\n` 参与切句 | 需先取证（统计真实回答的换行频率）再决定，**不凭猜改切句规则**（会动到流水线重叠的核心） |
| #7/#13/#14/#15/#16/#17/#19 | 可维护性/微优化：对项目要证明的东西零加分、改动面不小，**本轮不做** |

### 三、验证证据

- [x] pytest **53 passed**；`tsc --noEmit` exit=0
- [x] **真实链路完整一轮**（`curl -N` 读 SSE 到底）：22 `brain_token` + 65 `tts_audio` + `done`，`state_after=listening`；
      **`lip_dropped=4`**（`lip_chunks=13`、队列上限 8）→ **背压逻辑实测生效**（本次 `lip_error` 为"云 GPU 未开"，非缺陷）
- [x] 打断回归 `eval/verify_interrupt_e2e.py` **PASS**（interrupt → 流 4ms 结束、后续 `tts_audio` 0 条、未发 done）
- [x] 新端点手测：命中返回 `state/history_len`，不存在的会话 **404**

## 阻塞与未完成

- **真人麦克风链路仍未验证**（脚本替代不了）：插话是否真能触发打断、**数字人外放是否自打断（依赖浏览器 AEC）**——
  清单见 `backend/eval/reports/asr_mic_runbook.md`。离线已证：无 AEC 时外放音频命中打断判据 37 帧 / 触发 17 次。
- 服务端会话持久化（重档）未做；后端重启后旧会话失效，前端只能保留历史展示。
- 口型链路：`lip.queue_max` 背压已加，但**真实云 GPU 下是否真的会积压**仍未测（RUNBOOK §3.14 待验项：单片生成耗时 <1000ms）。
- 外部清单里"需取证"的 #3 未做；可维护性类（#13~#17、#19）明确不做。

## 决策变更

无新增/取代 ADR。本轮全为实现层改动 + SPEC 版本推进（**v1.7 → v1.8**：会话探测端点、`done.lip_dropped`、契约标注、TTS 连接独占）。

> ⚠️ 日期说明：本文档创建于 2026-09-17。此前几份文档（PROGRESS-2026-09-15、SPEC v1.4~v1.7、台账若干行）标的是会话开始日 2026-09-15，实际写作跨至 09-16/09-17——**内容是准的，日期标注偏早**。未回改已提交的历史文件（改动面大于收益），在此留痕。

---

*纪律：本文档只记本阶段发生的**增量事件**（完成了什么、阻塞什么、决策变没变）。禁止粘贴"当前时刻全量快照"——快照/临时输出属于过程产物，放 `docs/_inbox/`，蒸馏后删除（见 docs-guide §5）。*
